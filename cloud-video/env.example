# Copy names into shell or app .env.local — never commit real values

CURSOR_API_KEY=
# https://cursor.com/dashboard/integrations

# Optional session env for the cloud VM (AFTERCUT film)
MINDS_BUILDER_API_KEY=
MINDS_DIRECTOR_MIND_ID=

# Generic freeform (JSON string in shell, not this file):
# CLOUD_ENV_VARS={"SOME_KEY":"..."}
